<?xml version="1.0" encoding="UTF-8"?>
<rss xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd"
     xmlns:jmutube="http://jmutube.cit.jmu.edu/" version="2.0">
<channel>
    <title>{{ title }}</title>
    <link>{{ url }}</link>
    <language>en-us</language>
    <itunes:owner>
        <itunes:name>{{ record.owner.get_full_name }}</itunes:name>
        <itunes:email>{{ record.owner.email }}</itunes:email>
    </itunes:owner>
    <jmutube:params autoplay="false" showbigplayheadtime="false"/>
{% for item in playlist %}
    <item>
        <title>{{ item.record.title }}</title>
        <description>{{ item.record.title }}</description>
        <enclosure url="{{ item.url }}" length="{{ item.size }}" type="{{ item.mimetype }}" />
    </item>
{% endfor %}
</channel>
</rss>
